services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - QWEN_API_KEY=${QWEN_API_KEY:-}
      - QWEN_MODEL=${QWEN_MODEL:-qwen-max}

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
